<div class="content">
    <div class="container-fluid">
        <div class="jumbotron" id="head-doc">
            <header>
                <h1>GymMember</h1>
            </header>
            <main>
                <p>{{message}}</p>
                <div class="row">
                    {{#if gymMember}}
                    {{#each gymMember}}
                    <div class="col-sm-3" id="activity">
                        <a href="/gymMember/view/{{this._id}}">
                            <p id="member">{{this.membername}}</p>
                            <hr>
                            <img src="../public/images/abs.jpg">
                        </a>
                    </div>
                    {{/each}}
                    {{/if }} 
                        <div class="col-sm-3" id="add">
                            <a href="gymMember/add"><img src="../public/images/add.png"></a>
                        </div>
                </div>
            </main>
        </div>
    </div>
</div>