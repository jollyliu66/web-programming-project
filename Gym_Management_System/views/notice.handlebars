<div class="content">
    <div class="container-fluid">
        <div class="jumbotron" id="head-doc">
            <header>
                <h1>Notice</h1>
            </header>
            <main>
                <p>{{message}}</p>
                <div class="row">
                    {{#if notice}}
                    {{#each notice}}
                    <div class="col-sm-3" id="activity">
                        <a href="/notice/view/{{this._id}}">
                            <p id="title">{{this.title}}</p>
                            <hr>
                            <img src="../public/images/abs.jpg">
                        </a>
                    </div>
                    {{/each}}
                    {{/if }} 
                        <div class="col-sm-3" id="add">
                            <a href="notice/add"><img src="../public/images/add.png"></a>
                        </div>
                </div>
            </main>
        </div>
    </div>
</div>