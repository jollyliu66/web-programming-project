<div class="content">
    <div class="container-fluid">
        <div class="jumbotron" id="head-doc">
            <header>
                <h1>Membership</h1>
            </header>
            <main>
                <p>{{message}}</p>
                <div class="row">
                    {{#if memberships}}
                    {{#each memberships}}
                    <div class="col-sm-3" id="activity">
                        <a href="/membership/view/{{this._id}}">
                            <p id="membershipname">{{this.membershipname}}</p>
                            <hr>
                            <img src="../public/images/abs.jpg">
                        </a>
                    </div>
                    {{/each}}
                    {{/if }} 
                        <div class="col-sm-3" id="add">
                            <a href="membership/add"><img src="../public/images/add.png"></a>
                        </div>
                </div>
            </main>
        </div>
    </div>
</div>